pcb-rnd 1.0.3
~~~~~~~~~~~~~
Version 1.0.3 is a major feature release with the following highlights:
 - embedded GPMI scripting
 - adjust object sizes to current routing style
 - static footprint tags: display tags and search by tag on the gtk HID
 - dynamic UI menus and actions (for the scripts)
 - new parametric footprints: qfp(), qfn(), plcc(), qsop(), bga()
 - better regex and stringlist based search-by-name

