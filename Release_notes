pcb-rnd 3.1.3
~~~~~~~~~~~~~

This release focuses on bugfixes and three features:

1. Major upgrade of the back annotation system (supported by sch-rnd):
   - break connection
   - create connection
   - remove net
   - add subcircuit
   - remove subcircuit
   - subcircuit attribute change or remove

2. Experimental support for librnd's new font engine (compile time option),
   including new fontmode features

3. Experimental lihata board v9 for the above features
