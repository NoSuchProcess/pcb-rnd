pcb-rnd 1.0.7
~~~~~~~~~~~~~
1.0.7 is a large scale cleanup release that touches most source files.
The three major changes:
 - remove non-c89 features (// comments, variable declaration in for()),
 - unify indentation and code formatting
 - reorganize the directory structure

It also features a lot of smaller fixes and removes most compiler warnings.


