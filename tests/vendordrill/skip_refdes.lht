ha:vendor_drill_map {
# skip test: if not skipped, round down to 0.3 mm

	units = mm
	round = down
	li:drillmap = {
		0.3
		100
	}

	li:skips = {
		refdes = {^U[1-3]$}               # Skip U1, U2, U3
	}
}
