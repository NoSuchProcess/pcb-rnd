     ha:Revert {
-     action = Load(Revert,none)
-     tip = {Revert to the layout stored on disk }
-     a = {<key>f;<key>r}
+     li:submenu {
+      ha:hello {
+       a = {<key>h;<key>h}
+       action = {message(hello) }
+      }
+      ha:world {
+       a = {<key>h;<key>w}
+       action = {message(world) }
+      }
+     }
     }
