- data model, ascii read, to test:
	- net based poly-pstk conn:
		- run multiple passes
		- connect lines/arcs too
		- convert thermal style (on polygon side, remember it in a poly attribute?)
	- standard text test (+TODO)
	- slot (bin load fails)
	- viatenting via_mask (TODO, test file fails)
	/ multiline text: only from version 15.1
	- text-on-all-layers (TODO - need 8 layers)
	- sample5 has non-uniform pstk template: TOPSHAPE, BOTSHAPE

- ascii code:
	- support text height
