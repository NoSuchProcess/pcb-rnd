$class import
$short import freepcb netlist
$long Import the netlist and footprints from freepcb format (exported by e.g. easyeda)
$state works
$fmt-native no
$fmt-feature-r freepcb netlist + footprint info
$package import-net
default buildin
autoload 1
