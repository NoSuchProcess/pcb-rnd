ha:rnd-menu-patch-v1 {
	prio=200
	li:patch {
		ha:append_menu {
			path = /main_menu/View
			li:submenu {
				ha:Show netnames when zoomed in = { checked=plugins/show_netnames/enable; action=conf(toggle, plugins/show_netnames/enable, design)}
			}
		}
	}
}
