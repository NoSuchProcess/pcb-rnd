$class import
$short import sch
$long Imports element and netlist data from the schematics (or some other source).
$state works
$fmt-native no
$fmt-feature-r import netlist and footprint info from gEDA schematics using gnetlist
$fmt-feature-r import netlist and footprint produced by an external command
default buildin
autoload 1
